{
  "name": "nemo-github",
  "version": "0.0.1",
  "description": "Github plugin for nemo",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src -d lib",
    "prebublish": "npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel-polyfill": "^6.7.4",
    "nemo-connector": "0.0.3",
    "superagent": "^1.8.2"
  },
  "devDependencies": {
    "babel-cli": "^6.6.5",
    "babel-preset-es2015": "^6.6.0"
  }
}
